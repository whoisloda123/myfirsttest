<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liucan.boot.persist.mybatis.mapper">
    <select id="selectByName" resultType="com.liucan.boot.persist.mybatis.mode.CommonUser">
        select * from common_user where name = #{name}
    </select>
</mapper>