<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.liucan.mybatis.dao.DaoMapper">
    <resultMap id="UserInfoResultMap" type="com.liucan.mybatis.mode.UserInfo">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Sun May 13 14:12:45 CST 2018.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="user_name" jdbcType="VARCHAR" property="userName"/>
        <result column="phone" jdbcType="VARCHAR" property="phone"/>
        <result column="email" jdbcType="VARCHAR" property="email"/>
        <result column="register_time" jdbcType="TIMESTAMP" property="registerTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>

    <select id="getUserEmail" parameterType="java.lang.String" resultType="java.lang.String">
        select email
        from user_info
        <where>
            <if test="userId">
                user_id = #{userId}
            </if>
            <if test="phone">
                and phone = ${phone}
            </if>
        </where>
    </select>

    <!--choose, when, otherwise 像Java 中的 switch 语句-->
    <select id="getUserName" parameterType="java.lang.String" resultType="java.lang.String">
        select user_name
        from user_info
        where user_id = #{userId}
        <choose>
            <when test="phone">
                and phone = ${phone}
            </when>
            <when test="email">
                and email = ${email}
            </when>
            <otherwise>
                and id = 8
            </otherwise>
        </choose>
    </select>

    <update id="updateUser" parameterType="java.lang.String">
        update user_info
        <set>
            <if test="phone != null">phone = ${phone},</if>
            <if test="email != null">email = ${email},</if>
        </set>
        where user_id = #{userId}
    </update>

    <!--foreach-->
    <select id="selectUserInfoInUids" parameterType="list" resultMap="UserInfoResultMap">
        select * from user_info
        <if test="uids != null">
            where user_id in
            <foreach collection="uids" index="index" item="uid" open="(" separator="," close=")">
                ${uid}
            </foreach>
        </if>
    </select>

</mapper>